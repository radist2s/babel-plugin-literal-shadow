import {ComponentType} from 'react'

export default function literalShadow(variable: ComponentType | string): string
export default function literalShadow(strings: string, arg: ComponentType | string,  ...restArgs: any[]): string
