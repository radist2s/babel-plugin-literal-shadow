/**
 * @param {(string|ComponentType)[]} strings
 * @param args
 * @return {string}
 */
export default function literalShadow(strings, ...args) {
    throw new Error('This function should not exist in compiled code. Make sure babel is using `@radist2s/babel-plugin-literal-shadow/babel`')
}
